<% 
	unless voteable.user == current_user ||
	!logged_in? ||
	current_user.voted_on?(voteable)
%>

	<%= link_to '<span class="accent">APPLAUD</span> THIS OUTFIT', :action => "vote_for", :id => voteable.id %>
    <br />
	<%= link_to '<span class="accent">DISAPPROVE</span> THIS OUTFIT', :action => "vote_against", :id => voteable.id %>
	<br />
<% end %>

<% if current_user %>
	<%= link_to '<span class="accent">FLAG</span> AS INAPPROPRIATE', :action => "flag_outfit", :id => voteable.id %>
<% end %>
</p>


